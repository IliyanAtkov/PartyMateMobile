<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="pageLoaded">
	<Page.actionBar>
   		<ActionBar title="Clubs">
        	<ActionBar.actionItems>
         		<ActionItem ios.systemIcon="15" android.systemIcon = "ic_menu_refresh" />
      		</ActionBar.actionItems>
    	</ActionBar>
  	</Page.actionBar>
	<StackLayout>
    	<SearchBar hint="SEARCH" id="searchBar" />
    	<StackLayout  orientation="horizontal">
    		<Image src="{{ clubImage }}" stretch="none"/> 
    		<Label text="{{ clubText }}" textWrap="true" />
    	</StackLayout>
    	 <ListView items="{{ clubs }}" itemTap="tapped">
    	 	   <ListView.itemTemplate>
    	 	   	<StackLayout orientation="horizontal">
   				    <Image src="{{ image }}" stretch="none" />
   				    <Label text="{{ name }}" stretch="none" />
   				</StackLayout>
   			 </ListView.itemTemplate>
    	 </ListView>
  	</StackLayout>
</Page>